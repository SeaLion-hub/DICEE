# DICEE 백엔드 배포용 최소 의존성 (계획 1~3단계 기준).
# FastAPI·Celery·크롤러(requests+BS4)·Auth(Google)·DB(psycopg)·Sentry만 포함.

# Web
fastapi==0.115.6
uvicorn==0.32.1

# DB (비동기: FastAPI / 동기: Celery 워커·Alembic) — psycopg만 사용.
sqlalchemy==2.0.36
psycopg[binary]==3.3.2
alembic==1.14.0
pgvector==0.4.2

# Config & schemas
pydantic==2.11.7
pydantic-settings==2.13.0
python-dotenv==1.1.1

# Celery & Redis
celery==5.6.2
redis==5.2.1

# Crawlers (requests + BeautifulSoup) & retry (tenacity)
beautifulsoup4==4.13.5
requests==2.32.5
tenacity==9.0.0

# Auth (Google OAuth, JWT)
httpx==0.28.1
PyJWT==2.10.1
google-auth==2.40.3
pyjwt-key-fetcher==0.8.0

# AI (Instructor + Gemini structured output). from_provider("google/...") uses GOOGLE_API_KEY or api_key.
instructor[google-genai]>=1.0.0

# Monitoring
sentry-sdk==2.18.0

# Logging (structured)
structlog==25.5.0
